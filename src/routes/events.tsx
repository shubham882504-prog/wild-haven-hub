import { createFileRoute } from "@tanstack/react-router";
import { ArrowUpRight, CalendarDays } from "lucide-react";
import { PageIntro } from "@/components/site-shell";
import { events } from "@/lib/sanctuary-data";
export const Route = createFileRoute("/events")({ head: () => ({ meta: [{ title: "Events & Field Notes — Aranya Wildlife Sanctuary" }, { name: "description", content: "Join sanctuary walks, conservation talks, family programs, and fundraising events at Aranya." }, { property: "og:title", content: "Events & Field Notes — Aranya" }, { property: "og:description", content: "Upcoming ways to learn, gather, and support wildlife." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: EventsPage });
function EventsPage(){return <><PageIntro eyebrow="Events & news" title="Gather for the wild."><p>Walk with naturalists, hear from field teams, and join a community that cares deeply about the living world.</p></PageIntro><section className="section-shell"><div className="grid gap-5">{events.map(e=><article key={e.title} className="card-lift grid gap-5 rounded-lg border border-border bg-card p-6 sm:grid-cols-[100px_minmax(0,1fr)_auto] sm:items-center"><div className="border-b border-border pb-4 sm:border-b-0 sm:border-r sm:pb-0"><CalendarDays className="mb-3 size-5 text-clay"/><strong className="font-display text-xl">{e.date}</strong></div><div className="min-w-0"><p className="eyebrow text-clay">{e.type} · {e.time}</p><h2 className="mt-2 font-display text-2xl">{e.title}</h2><p className="mt-2 max-w-2xl leading-7 text-muted-foreground">{e.text}</p></div><ArrowUpRight className="hidden size-5 text-primary sm:block"/></article>)}</div></section></>}