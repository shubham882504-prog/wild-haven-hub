import { createFileRoute } from "@tanstack/react-router";
import { PageIntro } from "@/components/site-shell";
import { residents } from "@/lib/sanctuary-data";

export const Route = createFileRoute("/animals")({ head: () => ({ meta: [{ title: "Meet Our Residents — Aranya Wildlife Sanctuary" }, { name: "description", content: "Meet the rescued elephants, tigers, bears, and native birds who call Aranya home." }, { property: "og:title", content: "Meet Our Residents — Aranya" }, { property: "og:description", content: "Every resident has a story, a personality, and a home for life." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: AnimalsPage });
function AnimalsPage(){return <><PageIntro eyebrow="Our residents" title="Every life has a story."><p>Meet a few of the remarkable animals who have found safety, companionship, and a home for life at Aranya.</p></PageIntro><section className="section-shell"><div className="grid gap-7 sm:grid-cols-2">{residents.map((animal)=><article key={animal.name} className="card-lift overflow-hidden rounded-lg bg-card"><img src={animal.image} alt={`${animal.name}, ${animal.species}`} loading="lazy" width={1024} height={768} className="aspect-[4/3] w-full object-cover"/><div className="p-7"><p className="eyebrow text-clay">{animal.species}</p><h2 className="mt-2 font-display text-3xl">{animal.name}</h2><p className="mt-3 leading-7 text-muted-foreground">{animal.bio}</p></div></article>)}</div></section></>}